import React from 'react';
import uLogo from "../../assets/u.png"

const Logo = ({ width = '100px', height = 'auto', alt = 'Logo' }) => {
  return (
    <img 
      src={uLogo} 
      alt={alt} 
      style={{ width, height }} 
    />
  );
};

export default Logo;
